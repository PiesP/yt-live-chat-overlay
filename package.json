{
  "name": "yt-live-chat-overlay",
  "version": "0.43.0",
  "description": "Nico-nico style comment overlay for YouTube live chat (100% local processing)",
  "type": "module",
  "private": true,
  "scripts": {
    "preinstall": "test -f packages/core/package.json || { echo '⚠️  Submodule not initialized. Run: git submodule update --init'; exit 1; }",
    "build": "vite build",
    "build:dev": "vite build --mode development",
    "dev": "vite build --mode development --watch",
    "check": "tsc --noEmit",
    "check:extension": "tsc -p tsconfig.extension.json --noEmit",
    "typecheck": "pnpm -s check",
    "knip": "knip --files --exports --dependencies --reporter compact --max-show-issues 50",
    "knip:full": "knip --files --exports --dependencies --reporter compact --max-show-issues 50",
    "lint": "biome lint .",
    "lint:fix": "biome lint --write .",
    "fmt": "biome check --linter-enabled=false .",
    "fmt:fix": "biome check --write --linter-enabled=false .",
    "fmt:check": "pnpm -s fmt",
    "circular": "dpdm --tsconfig tsconfig.json --circular --no-tree --no-warning src/**/*.ts",
    "check:test": "tsc -p tsconfig.test.json --noEmit",
    "check:e2e": "tsc -p tsconfig.e2e.json --noEmit",
    "quality": "pnpm -s fmt && pnpm -s lint && pnpm -s check && pnpm -s check:extension && pnpm -s check:test && pnpm -s check:e2e && pnpm -s check:i18n && pnpm -s circular && pnpm -s knip && pnpm -s quality:nose",
    "quality:fix": "pnpm -s fmt:fix && pnpm -s lint:fix && pnpm -s check && pnpm -s circular && pnpm -s knip",
    "quality:nose": "bash -c 'if command -v nose &>/dev/null; then nose query src --baseline .nose-baseline.json --fail-on new; else echo \"[nose] not installed — skipping\"; fi'",
    "check:extension-artifacts": "node --experimental-strip-types scripts/check-extension-artifacts.ts",
    "verify": "pnpm -s quality && pnpm -s build:all:ci",
    "clean": "rimraf dist",
    "validate:consistency": "vitest run test/consistency/",
    "check-versions": "node --experimental-strip-types scripts/check-versions.ts",
    "check-i18n": "node --experimental-strip-types scripts/check-i18n-parity.ts",
    "sync-versions": "node --experimental-strip-types scripts/sync-versions.ts",
    "prebuild": "pnpm -s check-versions && pnpm -s check-i18n && pnpm -s quality",
    "prebuild:extension": "pnpm -s check-versions && pnpm -s check-i18n && pnpm -s quality",
    "prebuild:extension:firefox": "pnpm -s check-versions && pnpm -s check-i18n && pnpm -s quality",
    "build:ci": "vite build",
    "build:extension:ci": "vite build --config vite.config.extension.ts && vite build --config vite.config.extension.cs.ts && vite build --config vite.config.extension.page.ts && cp extension/manifest.json dist-extension/manifest.json && cp -r extension/icons dist-extension/",
    "build:extension": "pnpm -s prebuild:extension && vite build --config vite.config.extension.ts && vite build --config vite.config.extension.cs.ts && vite build --config vite.config.extension.page.ts && cp extension/manifest.json dist-extension/manifest.json && cp -r extension/icons dist-extension/",
    "build:extension:dev": "vite build --mode development && vite build --config vite.config.extension.ts --mode development && vite build --config vite.config.extension.cs.ts --mode development && vite build --config vite.config.extension.page.ts --mode development && cp extension/manifest.json dist-extension/manifest.json && cp -r extension/icons dist-extension/",
    "build:extension:firefox:ci": "vite build --config vite.config.extension.firefox.ts && vite build --config vite.config.extension.cs.firefox.ts && vite build --config vite.config.extension.page.firefox.ts && cp extension/manifest.firefox.json dist-extension-firefox/manifest.json && cp -r extension/icons dist-extension-firefox/",
    "mut": "pnpm exec stryker run --reporters=progress",
    "mut:fast": "pnpm exec stryker run stryker.conf.fast.json --reporters=progress",
    "build:extension:firefox": "pnpm -s prebuild:extension:firefox && vite build --config vite.config.extension.firefox.ts && vite build --config vite.config.extension.cs.firefox.ts && vite build --config vite.config.extension.page.firefox.ts && cp extension/manifest.firefox.json dist-extension-firefox/manifest.json && cp -r extension/icons dist-extension-firefox/",
    "build:all:ci": "pnpm -s check-versions && pnpm -s check-i18n && pnpm -s build:ci && pnpm -s build:extension:ci && pnpm -s build:extension:firefox:ci && pnpm -s check:extension-artifacts",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:cov": "vitest run --coverage",
    "test:e2e": "playwright test --config test/e2e/playwright.config.ts",
    "test:e2e:headed": "pnpm test:e2e -- --headed",
    "pretest:e2e": "pnpm build:dev && node -e \"const fs=require('fs');if(!fs.existsSync('dist/yt-live-chat-overlay.dev.user.js')){console.error('ERROR: E2E userscript bundle was not produced at dist/yt-live-chat-overlay.dev.user.js');process.exit(1)}\"",
    "clean:all": "rimraf dist dist-extension dist-extension-firefox",
    "check:i18n": "node scripts/check-i18n.mjs"
  },
  "keywords": [
    "userscript",
    "youtube",
    "live-chat",
    "overlay",
    "nico-nico"
  ],
  "author": "PiesP",
  "license": "MIT",
  "homepage": "https://github.com/PiesP/yt-live-chat-overlay",
  "bugs": "https://github.com/PiesP/yt-live-chat-overlay/issues",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/PiesP/yt-live-chat-overlay.git"
  },
  "engines": {
    "node": ">=26.0.0",
    "pnpm": ">=11.2.2"
  },
  "packageManager": "pnpm@11.2.2",
  "volta": {
    "node": "26.5.0",
    "pnpm": "11.2.2"
  },
  "dependencies": {
    "@piesp/browser-core": "file:packages/core"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.5",
    "@playwright/test": "^1.61.1",
    "@stryker-mutator/core": "^9.6.1",
    "@stryker-mutator/vitest-runner": "^9.6.1",
    "@types/node": "^26.1.1",
    "@vitest/coverage-v8": "^4.1.10",
    "canvas": "^3.2.3",
    "dpdm": "^4.2.0",
    "jsdom": "^29.1.1",
    "knip": "^6.27.0",
    "rimraf": "^6.1.3",
    "typescript": "7.0.2",
    "vite": "^8.1.5",
    "vite-plugin-monkey": "^8.1.0",
    "vitest": "^4.1.10"
  }
}
